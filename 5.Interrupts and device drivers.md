## 5.Interrupts and device drivers

> 到底什么是中断???
>
> 中断来自于外部设备或者时钟,我们要停下来等待,所以叫中断.

中断和设备驱动.

> ​	中断对应的场景很简单，就是**硬件想要得到操作系统的关注**。例如**网卡收到了一个packet**，网卡会生成一个中断；**用户通过键盘按下了一个按键**，键盘会产生一个中断。操作系统需要做的是，保存当前的工作，处理中断，处理完成之后再恢复之前的工作。这里的保存和恢复工作，与我们之前看到的系统调用过程（注，详见lec06）非常相似。所以系统调用，page fault，中断，都使用相同的机制。

驱动程序对于特定的设备进行管理.驱动程序编写难度很大,因为程序和驱动一般都会一起运行.

上半部分进行系统调用,下半部分会陷入中断代码:

> ​	许多设备驱动程序在**两种环境**中执行代码：**上半部分在进程的内核线程中运行**，下半部分在**中断时**执行。上半部分通过**系统调用**进行调用，如希望设备执行I/O操作的`read`和`write`。这段代码可能会要求硬件执行操作（例如，要求磁盘读取块）；然后代码等待操作完成。最终设备完成操作并引发中断。驱动程序的**中断处理程序**充当下半部分，计算出已经完成的操作，如果合适的话唤醒等待中的进程，并告诉硬件开始执行下一个正在等待的操作。

### 5.1 Code: Console input

我们控制台程序的输入.

UART串口硬件接收输入的字符--->read读取console的字符.

​	每个byte输入都会生成一个中断,输入的字符会交给consoleintr,它会把这些字符积累在cons.buf,对于某些字符会进行特殊的处理,调用一个等待的consoleread把这些字符复制到用户空间,并且返回到用户空间.

### 5.2 Code: Console output

控制台的输出问题.

write系统调用,对于console的fd进行写入.

什么是I/O的并发,本质还是利用了buffer来作为沟通的桥梁.

> ​	需要注意，这里的一般模式是通过**缓冲区**和**中断机制**将设备活动与进程活动解耦。即使没有进程等待读取输入，控制台驱动程序仍然可以处理输入，而后续的读取将看到这些输入。类似地，进程无需等待设备就可以发送输出。这种解耦可以通过允许进程与设备I/O并发执行来提高性能，当设备很慢（如UART）或需要立即关注（如回声型字符(echoing typed characters)）时，这种解耦尤为重要。这种想法有时被称为I/O并发.

### 5.3 Concurrency in drivers

驱动中的并发.

**多个进程和一个驱动程序**的并发竞争的问题.

### 5.4 Timer interrupts

**定时器**中断.

​	维持时钟进行切换,维护时钟,不允许某个进程一直占有大量的计算资源.---进行**yield**--->触发调度器.

每次会维护一个全局时钟的计数.

​	我们上面的读取都是利用buffer,利用软件控制,就叫程序I/O,这样的速度比较慢.

> ​	需要高速移动大量数据的设备通常使用直接内存访问（**DMA**）。DMA设备硬件直接将传入数据写入内存，并从内存中读取传出数据。现代磁盘和网络设备使用DMA。DMA设备的驱动程序将在RAM中准备数据，然后使用对控制寄存器的单次写入来告诉设备处理准备好的数据。

​	一个字节一个中断,但是中断具有很高的开销,很慢,我们想尽可能的减少中断.

> ​	因此，如网络和磁盘控制器的高速设备，使用一些技巧减少中断需求。**一个技巧是对整批传入或传出的请求发出单个中断**。**另一个技巧是驱动程序完全禁用中断**，并定期检查设备是否需要注意。这种技术被称为**轮询（polling）**。如果设备执行操作非常快，轮询是有意义的，但是如果设备大部分空闲，轮询会浪费CPU时间。一些驱动程序根据当前设备负载在轮询和中断之间动态切换。

​	那么第一种技巧其实也是DPDK库的工作原理之一,我们会对于一个网卡上的多个队列进行polling,一次会处理很多的数据包,这样可以减少中断带来的开销.--->CPU是主动的,而不是被动的等待中断来.

> ​	UART驱动程序首先将传入的数据复制到内核中的缓冲区，然后复制到用户空间。这在低数据速率下是可行的，但是这种双重复制会显著降低快速生成或消耗数据的设备的性能。一些操作系统能够直接在用户空间缓冲区和设备硬件之间移动数据，通常带有**DMA**。

​	同样的,DPDK实现了**0 copy**,直接在用户空间对于数据包进行处理,而不用把数据从内核缓冲区接收之后再次复制到用户空间.

这样就串起来了,这才是OS.

### Notes:

#### 真实的操作系统内存

大部分都会被buf或者cache占用,几乎没有空闲.

```sh
top - 20:33:03 up 12:37,  1 user,  load average: 0.20, 0.37, 0.39
Tasks: 499 total, 1 running, 498 sleep, 0 d-sleep, 0 stopped, 0 zombie
%Cpu(s):  1.0 us,  0.4 sy,  0.0 ni, 98.3 id,  0.0 wa,  0.1 hi,  0.1 si,  0.0 st 
MiB Mem :  15674.9 total,    449.8 free,   7688.1 used,  10414.1 buff/cache     
MiB Swap:   4096.0 total,   4087.0 free,      9.0 used.   7986.9 avail Mem 
```

并且观察这些进程,**物理内存是远小于虚拟内存**的,这表明我们虚拟内存机制的作用.

```sh
    PID USER      PR  NI    VIRT    RES    SHR S  %CPU  %MEM     TIME+ COMMAND                                                         
  23299 ada       20   0 4687116 742800 410512 S   9.6   4.6  14:46.59 plasmashell                                                     
   1908 ada       -2   0 6124740 665860 367352 S   5.0   4.1  25:09.82 kwin_wayland                                                    
  34478 ada       20   0 1421000 382000 324888 S   1.7   2.4   0:03.30 konsole                                                         
  23346 ada       20   0  178948  36096  30792 S   0.7   0.2   1:04.58 ksystemstats                                                    
  33590 root      20   0       0      0      0 I   0.7   0.0   0:02.88 kworker/u128:0-kvfree_rcu_reclaim                               
    829 dbus      20   0    9096   5128   2324 S   0.3   0.0   0:11.36 dbus-broker                                                     
  30631 ada       20   0   33.1g 537180 431724 S   0.3   3.3   0:44.41 vivaldi-bin                                                     
  31645 ada       20   0 1393.7g 210004 141696 S   0.3   1.3   0:13.13 code                                                            
  33591 root      20   0       0      0      0 I   0.3   0.0   0:04.98 kworker/u128:2-events_unbound                                   
  34435 root       0 -20       0      0      0 I   0.3   0.0   0:01.08 kworker/u129:0-i915_flip                                        
  34707 ada       20   0   14292   9764   7500 R   0.3   0.1   0:00.58 top                                                             
      1 root      20   0   22996  14316   9792 S   0.0   0.1   0:04.04 systemd                                                         
```

#### 对于硬件的理解

中断 和 syscall的区别:

1.中断和当前CPU上的进程无关,但是系统调用,会在当前进程的context下进行.

2.CPU和设备上的工作是 **并行** 的,我们要处理这一点.

驱动编程的复杂性需要考虑.

> - console中的提示符“$ ”是如何显示出来的
> - 如果你在键盘输入“ls”，这些字符是怎么最终在console中显示出来的。

这其实就是input和output的原理性问题.

如下图,**PLIC**管理来自于**设备的中断问题**:--->还要对**PLIC**进行编程.

![img](https://mit-public-courses-cn-translatio.gitbook.io/mit6-s081/~gitbook/image?url=https%3A%2F%2F1977542228-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-legacy-files%2Fo%2Fassets%252F-MHZoT2b_bcLghjAOPsJ%252F-MNOedUoWwf-B8e0x8xX%252F-MNQmPRa0bzPQ99lcE2p%252Fimage.png%3Falt%3Dmedia%26token%3D35741a0d-0fc6-42e9-a841-15537f9fdaf4&width=768&dpr=4&quality=100&sign=d0223e4e&sv=2)

​	PLIC会把中断路由给某个CPU core来进行处理,如果CPU全部都在处理中断,那么就暂时保存这个中断的状态.

> - PLIC会通知当前有一个待处理的中断
> - 其中一个CPU核会Claim接收中断，这样PLIC就不会把中断发给其他的CPU处理
> - CPU核处理完中断之后，CPU会通知PLIC
> - PLIC将不再保存中断的信息

#### 驱动程序

**管理设备**的这部分代码就叫做驱动.--->top/bottom

**top**上层代码,用户程序,作为接口.

**bottom**就是下层,负责对于这个中断的处理.

> ​	通常情况下，驱动中会有一些队列（或者说buffer），top部分的代码会从队列中读写数据，而Interrupt handler（bottom部分）同时也会向队列中读写数据。这里的队列可以将并行运行的设备和CPU解耦开来。

怎么控制这些物理设备的?

> ​	接下来我们看一下如何对设备进行编程。通常来说，编程是通过memory mapped I/O完成的。在SiFive的手册中，设备地址出现在物理地址的特定区间内，这个区间由主板制造商决定。操作系统需要知道这些设备位于物理地址空间的具体位置，然后再通过普通的load/store指令对这些地址进行编程。**load/store指令实际上的工作就是读写设备的控制寄存器。**例如，对网卡执行store指令时，CPU会修改网卡的某个控制寄存器，进而导致网卡发送一个packet。所以这里的load/store指令不会读写内存，而是会操作设备。并且你需要阅读设备的文档来弄清楚设备的寄存器和相应的行为，有的时候文档很清晰，有的时候文档不是那么清晰。

我们不实际编程,寄存器就像是设备上的一些按钮,我们准备好材料,然后按一些复杂的按钮来控制这些设备.

memory mapped I/O--->**把设备映射到不同的物理地址**.

```text
      +------------------------------------+
      |          UART Controller           |
      |------------------------------------|
      |  Receiver Holding Register (RHR)   | ← 接收缓冲
      |  Transmit Holding Register (THR)   | ← 发送缓冲
      |  Line Status Register (LSR)        | ← 状态寄存器
      |  Interrupt Enable Register (IER)   |
      |  Interrupt Identification Register |
      +------------------------------------+
```

#### 中断的原理

$符号的显示 ls的工作原理.

> ​	对于“ls”，这是用户输入的字符。键盘连接到了UART的输入线路，当你在键盘上按下一个按键，UART芯片会将按键字符通过串口线发送到另一端的UART芯片。另一端的UART芯片先将数据bit合并成一个Byte，之后再产生一个中断，并告诉处理器说这里有一个来自于键盘的字符。之后**Interrupt handler**会处理来自于UART的字符。

每个CPU上的进程在scheduler中都会先开中断,接着再运行.

```C
void
uartputc(int c)
{
  acquire(&uart_tx_lock);

  if(panicked){
    for(;;)
      ;
  }

  while(1){
    if(uart_tx_w == uart_tx_r + UART_TX_BUF_SIZE){
      // buffer is full.
      // wait for uartstart() to open up space in the buffer.
      sleep(&uart_tx_r, &uart_tx_lock);
    } else {
      uart_tx_buf[uart_tx_w % UART_TX_BUF_SIZE] = c;
      uart_tx_w += 1;
      uartstart();
      release(&uart_tx_lock);
      return;
    }
  }
}
```

这个函数会写入字符,

#### 中断的并发问题

1.设备和CPU同时运行,**producer**和**consumer**的并行.

2.内核有的时候需要临时关闭中断.

3.一个驱动的top部分写入buffer的时候,这个驱动的bottom部分可能处理另外的键盘读取的工作,这里会用到相同的buffer,所以我们使用了lock来控制这个问题.

> 学生提问：对于uartputc中的sleep，它怎么知道应该让Shell去sleep？
>
> Frans教授： sleep会将当前在运行的进程存放于sleep数据中。它传入的参数是需要等待的信号，在这个例子中传入的是uart_tx_r的地址。在uartstart函数中，一旦buffer中有了空间，会调用与sleep对应的函数wakeup，传入的也是uart_tx_r的地址。任何等待在这个地址的进程都会被唤醒。有时候这种机制被称为conditional synchronization。

根据是否有空间来进行唤醒的操作.

基本原理就是:

> ​	通过**buffer**将**consumer**和**producer**之间解耦，这样它们才能按照自己的速度，独立的并行运行。如果某一个运行的过快了，那么**buffer**要么是满的要么是空的，consumer和producer其中一个会**sleep**并等待另一个追上来。

> ​	对于一个高性能的网卡，如果有大量的包要传入，那么应该用polling。对于一些精心设计的驱动，它们会在**polling**和**Interrupt**之间动态切换（注，也就是网卡的NAPI）。

设备本身也会自己进行一些策略的处理.