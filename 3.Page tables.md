## 3.Page tables

页表,我们开始关注内存的问题.

为每个进程分配独立的地址空间,不会相互影响,并且解决内存大小的问题.

### 3.1 Paging hardware

硬件是怎么实现的?

As a reminder, RISC-V instructions (both user and kernel) manipulate virtual addresses.

The machine’s RAM, or physical memory, is indexed with physical addresses.

我们用页表来进行映射.

从虚拟地址到物理地址翻译的逻辑:

![image-20250919195010616](../../../mio/static/img/image-20250919195010616.png)

我们这里实现的是3级页表,多级页表是怎么实现的?

每个CPU有自己的satp,存放了root pagetable的地址.

注意这里L2是第一层.

![image-20250919195550431](../../../mio/static/img/image-20250919195550431.png)

如果没有找到对应的地址,就会引发缺页异常,从而陷入内核,调入地址再次进行访问.

flag就会有不同的用法,是否有效,读写权限,是否可访问,dirty--->cache的概念.

### 3.2 Kernel address space

内核的地址空间.

**每个进程分配一个页表来描述地址空间**,内核同理.

读写特定的物理地址和硬件进行交流.

内核地址空间--->进行直接映射:

![image-20250919200926402](../../../mio/static/img/image-20250919200926402.png)

直接映射很方便:

​	比如fork返回物理地址,那么copy的时候,直接就把这个物理地址当成虚拟地址处理即可,同样,这样syscall也更加方便:

​	the kernel itself is located at KERNBASE=0x80000000 in both the virtual address space and in physical memory. **Direct mapping simplifies kernel code that reads or writes physical memory.** For example, when fork allocates user memory for the child process, the allocator returns the physical address of that memory; fork uses that address directly as a virtual address when it is copying the parent’s user memory to the child.

Guard page是不是类似于canary:

​	The kernel stack pages. Each process has its own kernel stack, which is mapped high so that below it xv6 can leave an unmapped guard page. The guard page’s PTE is invalid (i.e., PTE_V is not set), so that if the kernel overflows a kernel stack, it will likely cause an exception and the kernel will panic. Without a guard page an overflowing stack would overwrite other kernel memory, resulting in incorrect operation. A panic crash is preferable.

在 xv6（RISC-V 版本）中，**trampoline 页**的作用主要有两个：--->相当于就是用来切换的.

1. **处理中断/系统调用**
   当用户程序执行 `ecall`（系统调用指令）或发生中断时，CPU 会切换到内核模式，但不会自动把用户栈/寄存器保存好。这时 trampoline 代码就会执行，它做的事包括：

   - 保存**用户寄存器到内核栈**
   - 切换到**合适的内核页表和内核栈**
   - 跳转到真正的**内核处理函数**

   这样，内核才可以在一个干净的环境下继续处理。

2. **返回用户态**
   当内核处理完系统调用/中断后，trampoline 代码还负责：

   - **恢复用户寄存器**
   - **切换回用户页表**
   - 执行 `sret`（RISC-V 的返回用户态指令）**回到用户空间**

### 3.3 Code: creating an address space

创建一个地址空间--->这里主要要看源代码.

### 3.4 Physical memory allocation

物理内存的分配.

4096bytes--->页是操作的最小单位.

空闲链表维护.

### 3.5 Code: Physical memory allocator

物理内存的分配.

### 3.6 Process address space

进程地址空间

进程具有分离的页表,切换的时候,页表也会切换:

​	Each process has a separate page table, and when xv6 switches between processes, it also changes page tables. As Figure 2.3 shows, a process’s user memory starts at virtual address zero and can grow up to MAXVA (kernel/riscv.h:348), allowing a process to address in principle 256 Gigabytes of memory.

分配的过程,当进程请求更多的用户内存的时候,kalloc就会分配一些page,然后把PTE加入表项.

​	When a process asks xv6 for more user memory, xv6 first uses kalloc to allocate physical pages. It then adds PTEs to the process’s page table that point to the new physical pages. Xv6 sets the PTE_W, PTE_X, PTE_R, PTE_U, and PTE_V flags in these PTEs. Most processes do not use the entire user address space; xv6 leaves PTE_V clear in unused PTEs.

用户process认为自己的空间无限,连续,并且独立.

​	We see here a few nice examples of use of page tables. First, different processes’ page tables translate user addresses to different pages of physical memory, so that each process has private user memory. Second, each process sees its memory as having contiguous virtual addresses starting at zero, while the process’s physical memory can be non-contiguous. Third, the kernel maps a page with trampoline code at the top of the user address space, thus a single page of physical memory shows up in all address spaces.

更细节的用户地址空间的组成,以及栈的初始化,所以这就是为什么入口是main函数.

![image-20250923161725715](../../../mio/static/img/image-20250923161725715.png)

多余内存分配,这里的**guard page**的道理和canary是类似的,用来检测栈的溢出.

​	To detect a user stack overflowing the allocated stack memory, xv6 places an invalid guard page right below the stack. If the user stack overflows and the process tries to use an address below the stack, the hardware will generate a page-fault exception because the mapping is not valid. A real-world operating system might instead automatically allocate more memory for the user stack when it overflows.

### 3.7 Code: sbrk

控制进程内存的大小

​	Sbrk is the system call for a process to shrink or grow its memory. The system call is implemented by the function growproc (kernel/proc.c:239). growproc calls uvmalloc or uvmdealloc, de pending on whether n is postive or negative. uvmalloc (kernel/vm.c:229) allocates physical memory with kalloc, and adds PTEs to the user page table with mappages. uvmdealloc calls uvmunmap (kernel/vm.c:174), which uses walk to find PTEs and kfree to free the physical memory they refer to.

### 3.8 Code: exec

exec是怎么实现的?

创建一个地址空间的user部分.为新的进程分配一个新的页表,分配内存,然后使用loadseg把执行文件加载到内存里.

在创建新内存镜像的过程中，如果 `exec` 发现错误（例如无效的程序段），它会跳转到标签 `bad`，释放新创建的镜像并返回 `-1`。`exec` 必须等到确定系统调用可以成功返回之后，才释放旧的镜像：如果旧镜像被提前释放，系统调用就无法向原进程返回 `-1`。`exec` 的所有错误都发生在创建新镜像的过程中。一旦镜像完整，`exec` 就可以提交（commit）新的页表（`kernel/exec.c:113`）并释放旧的页表（`kernel/exec.c:117`）。

dpdk:huagepages可以减少页表操作带来的开销.

复杂的内核会有更多类型的块的分配.

xv6没有malloc分配器,虽然我们在CSAPP写过.

### Notes:

#### 1.Intro

虚拟内存	页表--->映射关系--->进程的隔离性

#### 2.地址空间

隔离性:1.进程彼此不会相互影响2.如果进程出问题不会影响内核代码

进程有自己的地址空间--->强隔离性.

#### 3.页表

> 细节问题比较多.

硬件的实现 CPU + MMU(内存管理单元)

VA给MMU翻译,接着得到PA,用PA来访问物理内存.

page table被保存在内存中,MMU会在内存中查page table.

**每个进程会拥有一个SATP寄存器,它存放了这个进程表单在内存中的物理地址(最高级的表单,从上往下开始查询),那么当进程进行切换的时候,SATP的值就会产生切换,那么自然有了隔离性.**--->核心理念

接下来我们开始进化!

1.虚拟地址的映射,我们不会为每个地址都创建一个条目,而是为每个page创建一个条目.

一个page 4096Bytes

VA:index + offset index找到page的位置,offset决定是page中的哪一个字节.(**有时候你可能会搞混,这里的offset是一个字节在一个page中的偏移量,而不是某个page在某个地址下的偏移量**)

offset-->12bit--->决定了是哪个字节.

那么物理内存就是以page为粒度进行使用的

**xv6:**

VA:高25bit不使用,低39bit,前27bit作page索引,后12bit作offset.

PA:共56bit,前44bit作物理内存所有page的索引,后12bit作offset--->这本来就是自然而且连续的.

2.那么比如说,每个页都对应一个条目,那么一个进程就需要2^27个条目,多几个进程的话就会占用大量的物理内存,我们迎来了多级页表.

​	所以实际上，SATP寄存器会指向最高一级的page directory的物理内存地址，之后我们用虚拟内存中index的高9bit用来索引最高一级的page directory(注，2^9 = 512，正好可以索引到一条 PTE,这里的9bit就是作为偏移量来作用的)中的对应的表项，这样我们就能得到一个PPN，也就是物理page号。这个PPN指向了中间级的page directory(这个PPN,高44bit有数据,低12bit数据为0,而这就是下一级page directory的物理内存地址).

实际存在映射关系的时候才会被创建,节约了内存.--->**思想:按需分配**

映射的过程中offset会一直保留下去,在PTE中,**低位的一些bit会用来进行权限的控制**--->flag位.

> 学生提问：我想知道我们是怎么计算page table的物理地址，是不是这样，我们从最高级的page table得到44bit的PPN，然后再加上虚拟地址中的12bit offset，就得到了完整的56bit page table物理地址？
>
> Frans教授：我们**不会加上虚拟地址中的offset，这里只是使用了12bit的0。所以我们用44bit的PPN，再加上12bit的0，这样就得到了下一级page directory的56bit物理地址**。这里要求每个page directory都与物理page对齐（也就是page directory的起始地址就是某个page的起始地址，所以低12bit都为0）。

#### 4.页表缓存（Translation Lookaside Buffer）

TLB,在CSAPP中也被成为快表.

当处理器第一次查找一个虚拟地址时，硬件通过3级page table得到最终的PPN，TLB会保存虚拟地址到物理地址的映射关系。这样下一次当你访问同一个虚拟地址时，处理器可以查看TLB，TLB会直接返回物理地址，而不需要通过page table得到结果.

同样的,如果切换了page table,那么OS也应该清除TLB内部的缓存.

> 学生提问：在这个机制中，TLB发生在哪一步，是在地址翻译之前还是之后？
>
> Frans教授：整个CPU和MMU都在处理器芯片中，所以在一个RISC-V芯片中，有多个CPU核，MMU和TLB存在于每一个CPU核里面。RISC-V处理器有L1 cache，L2 Cache，有些cache是根据物理地址索引的，有些cache是根据虚拟地址索引的，由虚拟地址索引的cache位于MMU之前，由物理地址索引的cache位于MMU之后。

因为操作系统对于这里的地址翻译有完全的控制，它可以实现各种各样的功能。比如，当一个PTE是无效的，硬件会返回一个page fault，对于这个page fault，操作系统可以更新 page table并再次尝试指令。

陷入缺页异常之后就可以再进行重新的加载.

#### 5.Kernel Page Table

内核页表.

![image-20250919200926402](../../../mio/static/img/image-20250919200926402.png)

​	主板的设计人员决定了，**在完成了虚拟到物理地址的翻译之后，如果得到的物理地址大于0x80000000会走向DRAM芯片，如果得到的物理地址低于0x80000000会走向不同的I/O设备.**

​	地址0x1000是boot ROM的物理地址，当你对主板上电，主板做的第一件事情就是运行存储在boot ROM中的代码，当boot完成之后，会跳转到地址0x80000000，操作系统需要确保那个地址有一些数据能够接着启动操作系统.

- PLIC是中断控制器（Platform-Level Interrupt Controller）我们下周的课会讲。
- CLINT（Core Local Interruptor）也是中断的一部分。所以多个设备都能产生中断，需要中断控制器来将这些中断路由到合适的处理函数。
- UART0（Universal Asynchronous Receiver/Transmitter）负责与Console和显示器交互。
- VIRTIO disk，与磁盘进行交互。

> 学生提问：当读指令从CPU发出后，它是怎么路由到正确的I/O设备的？比如说，当CPU要发出指令时，它可以发现现在地址是低于0x80000000，但是它怎么将指令送到正确的I/O设备？
>
> Frans教授：你可以认为在RISC-V中有一个多路输出选择器（demultiplexer）.

在PHYSTOP之下,我们都使用的是直接映射.

> 这是众多你可以通过page table实现的有意思的事情之一。你可以向同一个物理地址映射两个虚拟地址，你可以不将一个虚拟地址映射到物理地址。可以是一对一的映射，一对多映射，多对一映射。XV6至少在1-2个地方用到类似的技巧。这的kernel stack和Guard page就是XV6基于page table使用的有趣技巧的一个例子。

同时你还可以设置**权限**的问题.

上面还有一段**free memory**,XV6使用这段free memory来存放用户进程的**page table**，**text**和**data**。

如果我们运行了非常多的用户进程，某个时间点我们会耗尽这段内存，这个时候fork或者exec会返回错误。

#### 6.函数分析

kvminit

### Lab:

> 还是要**认真阅读实验手册**,要不然做对了都不知道.

在用户态和内核态之间建立一个只读的内存共享区域,来加速,防止不断的进行切换的操作.

proc 结构是**内核态数据**，用户态无法直接读取，因此需要通过系统调用 **getpid** 来读到 **pid**(如果这样的话就要切换,但是仅仅是为了一个简单的读取)。

这里需要我们创建一个共享 **PTE**(其实就是用户态可以访问但是只读来控制权限)，将虚拟地址 **USYSCALL** 映射到 **pid** 的物理地址，这样用户态直接读取 **USYSCALL** 就可以获取到 **pid** 了。